import React from "react";
import { Container, AppTitle, Subtitle } from "./Title.styled";
const Title = () => {
  return (
    <Container>
      <AppTitle>Weather Finder</AppTitle>
      <Subtitle>Find out Temptation, condition and more...</Subtitle>
    </Container>
  );
};

export default Title;
